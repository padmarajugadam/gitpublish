


pipeline {
 agent any
	tools {
        maven 'maven3.6.1' 
    }
 stages {
 
 	stage('Demo') {
     	  steps {
       		echo 'Hello world'
       	        
               }
	              }
  
	 
	 stage('Build') { 
            steps {
                echo "maven build"
            }
        }
	
	 stage('Analysis') {
	 steps {
              echo "Analysis"
            }
	 }
	  stage("Quality Gate") {
            steps {
              echo "quality passed"
            }
          }
      
        }						   

    post {
        
        success {
	 sh 'git branch -a'
          
       sh 'git checkout develop'
           withCredentials([usernamePassword(credentialsId: 'gitpush', passwordVariable: 'pass', usernameVariable: 'name')]) {
            sh 'git push origin develop https://${name}:${pass}@github.com/padmarajugadam/gitpublish.git'
	    sh 'git log --graph --decorate --oneline'
           }
        }
	 failure {
            echo 'I failed :('
        }
    }
}
