pipeline {
 agent any
   stages {
       stage('Demo') {
     	  steps {
	     sh 'git branch'
       	   sh 'ls'
	   sh 'git branch --all'
	   echo 'Test checkout branch'
	   sh 'git checkout develop'
	
	              }
                }
		}
		}
